let num = Number(prompt("IStalgan son kiriting"));
let numSecond = Number(prompt("Istalgan son kiriting"));
if ( num > 0 == numSecond > 0 ) {
    document.write(`Kiritilgan ikkala son ham 0 dan katta`);
} 
else{
    document.write(`Kiritilgan sonlar a > 0 == b > 0 shartiga tug'ri kelmaydi`)
}